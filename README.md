# Edge Master

Static site for local demo. Deploy with GitHub Pages (branch `main`, root).

To publish:

1. Create a GitHub repo.
2. Push this folder to `main`.
3. Enable Pages in repo settings (select `main` / root).
